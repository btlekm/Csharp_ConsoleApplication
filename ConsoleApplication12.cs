using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace ConsoleApplication12
{
    class Program
    {
        static void Main(string[] args)
        {
            /*Klavyeden girilen iki sayı aralığındaki sayıları ekrana yazdıran programı for döngüsü kullanarak yazınız.(Sayı Girişlerinde Kısıtlama Yok.)*/
            int i, say1, say2, toplam, basla, bitis;
            toplam = 0;

            Console.Write("1.Sayıyı Girin:");
            say1 = Convert.ToInt32(Console.ReadLine());

        }
    }
}
